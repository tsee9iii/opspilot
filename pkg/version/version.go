// Package version is the single source of truth for OpsPilot service version
// strings reported in process metadata (MCP health, initialize, etc.). Keep
// version values here and reference them rather than duplicating literals.
package version

// Central is the OpsPilot Central version.
const Central = "0.2.4"

// MCP is the OpsPilot MCP server version.
const MCP = "1.1.0"
